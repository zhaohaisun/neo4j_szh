package org.example;

import static org.example.CSVImporter.neo4j_Bj;

public class HistoricalSnapshotQuery {
    // 实现历史快照查询
    /*
    {定义3.1：历史快照查询（Snapshot query）} 对于给定的时态属性图，返回在某时刻对应的静态图的快照。其形式化定义如下：
    $$snapshot(TG, tp, t) \Rightarrow \langle(entity_1, value_1), \cdots, (entity_n, value_n)\rangle$$
    其中$TG$为时态属性图，$tp$为查询的时态属性，$t$为时间点。返回一个由元组$(entity, value)$组成的集合，其中$entity$表示一个点/边，$value$为该点/边的$tp$属性在时间$t$的值。
    例如：对于北京市交通时态网络，查询早晨8点海淀区所有道路的拥堵状态。
     */

}
